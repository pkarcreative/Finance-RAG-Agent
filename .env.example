OPENAI_API_KEY=sk-your-key
# Optional: override the corpus location (defaults to ../finance_rag_corpus)
# CORPUS_DIR=/path/to/finance_rag_corpus
# Optional: override the chat model (defaults to gpt-4o-mini)
# LLM_MODEL=gpt-4o-mini
